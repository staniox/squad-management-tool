import React from 'react';
import Border from "./styles";

const Circle = () => {
    return (
        <Border>
            <div>
              <span>+</span>
            </div>
        </Border>

    );
};

export default Circle;
